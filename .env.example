# SQLite (default). For PostgreSQL use e.g. postgresql://user:pass@localhost:5432/dance_school
DATABASE_URL="file:./dev.db"

# Secret used to sign session tokens. Generate a strong random value for production:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="change-me"
